surf <- function(ord,...)
{
    UseMethod("surf")
}

plotid <- function(ord,...)
{
    UseMethod("plotid")
}

specid <- function(ord,...)
{
    UseMethod("specid")
}

hilight <- function(ord, ...)
{
    UseMethod("hilight")
}

chullord <- function(ord, ...)
{
    UseMethod("chullord")
}

density <- function(ord, ...)
{
    UseMethod("density")
}
